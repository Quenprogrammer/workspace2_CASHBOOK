import { CustomeDateFormatPipe } from './custome-date-format.pipe';

describe('CustomeDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
